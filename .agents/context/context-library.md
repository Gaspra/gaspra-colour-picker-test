# Context Library

Use this index to select the smallest relevant repository context for a prompt.

Every file and folder stored in `.agents/context` must have one entry below, except this index and `.gitkeep`. Keep the index up to date whenever context is added, changed, moved, or removed.

| Path | Type | Summary | Use when |
| --- | --- | --- | --- |
